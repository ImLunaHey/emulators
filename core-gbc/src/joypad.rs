// TODO: joypad (P1/JOYP 0xFF00).
//
// Empty placeholder struct so the `Gbc` god-struct can own a slot; the porting
// agent for this file replaces it with the real subsystem state + behavior.

#[derive(Default)]
pub struct Joypad;
