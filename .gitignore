node_modules
dist
.vite
.DS_Store
*.log
public/*.gba
public/*.sav
*.gba
*.sav
# ROMs / disc images — never commit commercial dumps (PS1 .bin/.cue, etc.)
*.bin
*.cue
public/*.bin
public/*.cue
*.nds
_*.png
tsconfig.tsbuildinfo
coverage

# rust core
core/target
# second core (Nintendo DS) build artifacts
core-nds/target
# wasm-pack output — built in CI (build:wasm) and locally; no longer committed
core/pkg
# rust mobile FFI crate (native libs / xcframework build output)
core-ffi/target
core-ffi/build
core-ffi/Cargo.lock
**/*.rs.bk

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example
